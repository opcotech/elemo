# Development
NODE_ENV=development

# Set to true only when the app is served over HTTPS. Production Vite builds
# inline NODE_ENV=production, so cookie Secure must not be derived from it or
# WebKit/Safari will drop session cookies on http://localhost.
ELEMO_COOKIE_SECURE=false

# Server-only OAuth configuration is written by scripts/setup.sh.
