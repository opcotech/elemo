content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/user.yaml'
        - required:
            - username
            - first_name
            - last_name
            - email
            - password
