content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/todo.yaml'
        - required:
            - title
            - priority
            - owned_by
            - completed
