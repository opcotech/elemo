content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/todo.yaml'
        - required:
            - title
            - priority
            - user_id
            - completed
